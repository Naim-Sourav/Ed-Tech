name: Build Android APK

on:
  push:
    branches: [ main ] 

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18 

      - name: Install Dependencies
        run: npm install --legacy-peer-deps

      - name: Install Capacitor Core and Android Packages
        run: npm install @capacitor/core @capacitor/android --legacy-peer-deps

      - name: Build Web Project
        run: npm run build 

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Create Capacitor Config if missing
        run: |
          if [ ! -f "capacitor.config.json" ] && [ ! -f "capacitor.config.ts" ]; then
            echo '{"appId": "com.porikkhangon.app", "appName": "Porikkhangon", "webDir": "dist"}' > capacitor.config.json
          fi

      - name: Add Android Platform if missing
        run: |
          if [ ! -d "android" ]; then
            npx @capacitor/cli add android
          fi

      - name: Sync Capacitor
        run: |
          npx @capacitor/cli sync

      # ১. এই অফিসিয়াল অ্যাকশনটি গ্রেডলের ফাইল লকিং ও ইন্টারনাল ক্যাশ বাগ দূর করবে
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      # ২. clean এবং --refresh-dependencies দিয়ে করপ্টেড সিকিউরিটি জার ফাইলটি নতুন করে জেনারেট করা হবে
      - name: Build Android APK
        run: |
          cd android
          ./gradlew clean assembleDebug --refresh-dependencies --no-daemon

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: my-web-app-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
