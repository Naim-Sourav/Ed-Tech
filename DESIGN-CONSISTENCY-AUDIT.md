# ডিজাইন অসামঞ্জস্য অডিট — Landing Page vs Home Dashboard

> **স্ট্যাটাস: ঠিক করা হয়েছে (২৩ সেপ্টেম্বর ২০২৬)।** নিচের সব P0/P1/P2 আইটেম কোডে প্রয়োগ করা হয়েছে —
> বিস্তারিত ফিক্স-লগ ডকুমেন্টের শেষে "✅ কী কী ঠিক করা হলো" সেকশনে। `npm run build` ✅, ২৭/২৭ টেস্ট ✅, `tsc --noEmit` ✅।
> নিচের বিশ্লেষণটি মূল (ফিক্সের আগের) অবস্থার রেকর্ড হিসেবে রাখা হলো।

তুলনা করা হয়েছে:
- **ল্যান্ডিং**: `components/LandingPage.tsx` + `components/landing/*` (`.pk-landing:not(.dash)` স্তর)
- **ড্যাশবোর্ড**: `components/HomeDashboard.tsx` (`.pk-landing.dash` স্তর) + শেল `components/Navigation.tsx`
- টোকেন: `tailwind.config.js`, `index.css`

সারাংশ: দুটো পেজ **একই ব্র্যান্ড পরিবার** দেখালেও বাস্তবে **তিনটি আলাদা ডিজাইন সিস্টেম** একসাথে চলছে —
(১) প্রিমিয়াম ল্যান্ডিং থিম, (২) ওয়ার্ম "dash" থিম, (৩) পুরনো gray/zinc অ্যাপ-শেল থিম। নিচে অগ্রাধিকার অনুযায়ী।

---

## 🔴 P0 — চোখে পড়ার মতো বড় অসামঞ্জস্য

### 1. বেস কালার দুটো পেজে আলাদা (কুল বনাম ওয়ার্ম কালো)
| টোকেন | ল্যান্ডিং | ড্যাশবোর্ড |
|---|---|---|
| ব্যাকগ্রাউন্ড | `#fafaf9` (`.pk-landing:not(.dash)`, index.css:433) | `#faf9f6` (`.pk-landing`, index.css:338) |
| টেক্সট/ইনk | `#101018` = `ink-950`, নীলচে-কালো | `#161210` = `ink.DEFAULT`, বাদামি-কালো |
| ডার্ক বিজি | `#0e0b09` | `#161210` |

ল্যান্ডিং হেডিং `text-ink-950` (কুল) আর ড্যাশবোর্ড `text-ink` (ওয়ার্ম) — পাশাপাশি রাখলে হেডিং-এর "কালো" মেলে না। tailwind.config-এ `ink.DEFAULT` আর `ink.950` দুটো সম্পূর্ণ ভিন্ন র‍্যাম্প (একটা ওয়ার্ম কাস্টম, একটা ঠান্ডা স্কেল) — এটাই মূল গোড়ার গলদ।

### 2. প্রাইমারি CTA-র রঙ উল্টে গেছে
- ল্যান্ডিং হিরো: `bg-gradient-to-r from-brand-600 to-brand-500` + সাদা টেক্সট (কমলা = প্রাইমারি অ্যাকশন) — `landing/Hero.tsx:113`
- ড্যাশবোর্ড হিরো ("মডেল টেস্ট দিন"): `bg-lime` (#ffb92e অ্যাম্বার) + কালো টেক্সট — `HomeDashboard.tsx:317`

অর্থাৎ ল্যান্ডিংয়ে যে রঙ শুধু accent/হাইলাইট, ড্যাশবোর্ডে সেটাই প্রধান বাটন। লগইনের পর ইউজার "প্রাইমারি অ্যাকশন কোনটা" শেখা ভুলে যায়।

### 3. রুট ফন্ট-সাইজ আলাদা → পুরো স্কেল ১০–১৬% ছোট হয়ে যায়
`index.css:65` → `html { font-size: 15px }`, মোবাইলে `13.5px`।
`LandingPage.tsx:40` মাউন্টে `html.pk-landing-root` যোগ করে সেটাকে **16px** করে দেয় (index.css:427)।
ফলে একই `text-base`/`p-4` ল্যান্ডিংয়ে বড়, ড্যাশবোর্ডে ছোট। মোবাইলে পার্থক্য ১৬.৭% — টাইপ ও স্পেসিং রিদম ভাঙে। উপরন্তু `index.css:600-638`-এর মোবাইল স্পেসিং কমপ্রেশন শুধু `:not(.dash)`-এ, ড্যাশবোর্ডে নেই।

### 4. স্ট্রিক মোডাল পুরোপুরি ব্র্যান্ডের বাইরে (Duolingo প্যালেট)
`HomeDashboard.tsx:608+` এ হার্ডকোডেড হেক্স: `#131F24`, `#202F36`, `#162127`, `#FF9600`, `#FFB800`, `#38BDF8`।
- ব্র্যান্ড কমলা `#ff5200` নয়, `#FF9600`; গোল্ড `#ffb92e` নয়, `#FFB800`; আর `#38BDF8` নীল তো প্যালেটেই নেই।
- মোডালটি **লাইট মোডেও জোর করে ডার্ক** — বাকি ড্যাশবোর্ড তখন ওয়ার্ম পেপার। থিম ভাঙে।
- ক্যালেন্ডার হার্ডকোডেড `"জুন ২০২৬"`, `Array.from({length:30})`, `isToday = i === 6` — ডেটা নয়, ডামি; মাস-নেভিগেশন বাটন দুটো কিছুই করে না।

### 5. অ্যাপ-শেল (Navigation.tsx) সম্পূর্ণ ভিন্ন প্যালেটে
সাইডবার/নোটিফিকেশন প্যানেল: `bg-white dark:bg-black`, `border-gray-200`, `text-zinc-400`, `bg-orange-50/text-orange-700`।
কিন্তু যে ড্যাশবোর্ড এটি ঘিরে রাখে সেটি `bg-paper #faf9f6 / dark:bg-ink #161210` ওয়ার্ম। ডার্ক মোডে **খাঁটি কালো নেভ + ওয়ার্ম বাদামি কনটেন্ট** পাশাপাশি — সবচেয়ে দৃশ্যমান মিসম্যাচ। অ্যাকসেন্টও দুই রকম: শেলে `orange-400/500` (Tailwind), পেজে `brand #ff5200`।

---

## 🟠 P1 — টাইপোগ্রাফি ও ব্র্যান্ডিং

### 6. `font-display` ড্যাশবোর্ডে অন্য ফন্টে রেন্ডার হচ্ছে (বাগ)
`index.css:439` — `.pk-landing:not(.dash) .font-display { font-family: 'Anek Bangla' … }`।
`:not(.dash)` থাকায় ড্যাশবোর্ডে এই রুল প্রযোজ্য নয়, ফলে `font-display` পড়ে যায় Tailwind-এর ডিফল্ট `"Bricolage Grotesque", Outfit, Inter`-এ — যেগুলোতে **বাংলা গ্লিফ নেই**। ড্যাশবোর্ডে `font-display` ৬ জায়গায় বাংলা সংখ্যা (`toBengaliNumber`) দেখাতে ব্যবহৃত (লাইন 287, 309, 392, 476, 581) → fallback ফন্টে রেন্ডার, ল্যান্ডিংয়ের সংখ্যার সাথে মেলে না।

### 7. তিন রকম হেডিং ফন্ট
- ল্যান্ডিং: সব হেডিং `font-display` (Anek Bangla, sans)
- ড্যাশবোর্ড: `font-bangla` = **Noto Serif Bengali (সেরিফ)** — লাইন 276, 440, 466
- বাকি অ্যাপ: `Hind Siliguri` / `Inter`

একই ব্র্যান্ডে sans hero বনাম serif hero — স্পষ্ট অসামঞ্জস্য।

### 8. লোগো লকআপ দুই রকম
- ল্যান্ডিং/Auth: `landing/Logo.tsx` → কালো রাউন্ডেড টাইলে `p-logo-mark.png` + টেক্সট wordmark `পরীক্ষা<span class="text-gradient">ঙ্গন</span>`
- ড্যাশবোর্ড টপ-বার: `letterlogo.svg` / `letterlogo-white.svg` (আলাদা আর্টওয়ার্ক, টাইল নেই) — লাইন 226-239

দুটো ভিন্ন ব্র্যান্ড মার্ক, ভিন্ন উচ্চতা (`size-10` বনাম `h-10 md:h-12`)।

### 9. ওজন ও মাইক্রো-লেবেল স্টাইল
ল্যান্ডিং: `font-semibold`/`font-bold`, eyebrow = `SectionTag` পিল (`helpers.tsx:83`)।
ড্যাশবোর্ড: `font-black` + `uppercase tracking-[0.18em]` ক্ষুদ্র লেবেল (`text-[9px]`, `text-[10px]`, `text-[11px]`) — ল্যান্ডিংয়ে এই প্যাটার্ন একেবারেই নেই, আর `SectionTag` ড্যাশবোর্ডে একবারও ব্যবহৃত হয়নি (সেখানে গ্রেডিয়েন্ট-বার হেডিং, লাইন 338)।

---

## 🟡 P2 — সারফেস, রেডিয়াস, মোশন

### 10. কার্ড সারফেসের ভাষা আলাদা
| | ল্যান্ডিং | ড্যাশবোর্ড |
|---|---|---|
| কার্ড | `bg-white/80..85` + `border-ink-100` + `backdrop-blur` + `card-shine` হোভার | সলিড `bg-white` / টিন্ট `bg-mint, bg-cream, bg-lime-soft, bg-amber-soft` + `ring-1 ring-ink/8` |
| হোভার | `-translate-y-1.5/-2` + `duration-500` + কমলা গ্লো | `-translate-y-1` + `duration-300` |

`mint/cream/amber-soft/lime-soft` টিন্ট টাইল ল্যান্ডিংয়ে প্রায় নেই; আবার `card-shine`, `card-ring`, `btn-shine`, `dot-grid`, `glow-brand`, `text-gradient` ইউটিলিটিগুলো `:not(.dash)`-এ স্কোপড বলে ড্যাশবোর্ডে **ব্যবহার করলেও কাজ করবে না**।

### 11. বর্ডার-রেডিয়াস টোকেনহীন
ল্যান্ডিং: `1.6rem`, `1.8rem`, `2rem`, `26px`, `28px`, `2.5rem`।
ড্যাশবোর্ড: `2rem`, `1.75rem`, `3xl (1.5rem)`, `2xl`।
কোনো শেয়ার্ড স্কেল নেই — `1.75rem` শুধু ড্যাশবোর্ডে, `1.6rem` শুধু ল্যান্ডিংয়ে।

### 12. মোশন টোকেন ডুপ্লিকেট ও ভিন্ন
- `landing/helpers.tsx:11` → `EASE = [0.22, 1, 0.36, 1]`, ডিউরেশন 0.8–0.9s
- `HomeDashboard.tsx:41` → `EASE = [0.16, 1, 0.3, 1]`, ডিউরেশন 0.4s

একই নামে দুটি আলাদা কনস্ট্যান্ট। ট্রানজিশনও 500ms বনাম 300ms।

### 13. ডার্ক-মোড টগলের আচরণ দুই রকম
- অ্যাপ: ৩-স্টেট `light → dark → system` (`App.tsx:457`)
- ল্যান্ডিং: ২-স্টেট `light ⇄ dark` (`useLandingTheme.tsx:62`), একই `themeMode` কী-তে লেখে → ল্যান্ডিংয়ে একবার টগল করলে ইউজারের `system` পছন্দ **স্থায়ীভাবে মুছে যায়**।
- ল্যান্ডিং মাউন্ট থাকা অবস্থায় `<html>.dark` দখল করে; আনমাউন্টে অ্যাপের `useEffect` আবার চালাতে হয় — রুট বদলে ফ্ল্যাশ হতে পারে।

---

## ♿ অ্যাক্সেসিবিলিটি (ধারাবাহিকতা-সংক্রান্ত)

14. **`text-mist` ডার্ক মোডে কনট্রাস্ট ফেল**: ড্যাশবোর্ডে ৭ বার ব্যবহৃত (`#6f655c`)। ডার্ক ওভাররাইড আছে শুধু `html.dark .pk-landing:not(.dash) .text-mist` (index.css:731) — `.dash`-এ নেই। `#6f655c` on `#161210` = **3.27:1**, ছোট টেক্সটে WCAG AA (4.5:1) ফেল। ল্যান্ডিংয়ে একই ক্লাস পাস করে (5.4:1) — মানে একই টোকেন দুই পেজে ভিন্ন ফল দিচ্ছে।
15. হিরোতে `text-white/50`, `text-white/60` লেবেল (লাইন 279, 291, 311) — ~4.5:1 এর বর্ডারলাইন, 9–10px সাইজে বাস্তবে অপাঠ্য।
16. স্কিপ-লিংক শুধু ল্যান্ডিংয়ে (`LandingPage.tsx:82`), ড্যাশবোর্ডে নেই।
17. ড্যাশবোর্ডের `<h1>` হলো ইউজারের নাম; কোনো সেকশনে `<h2>` র‍্যাঙ্কিং একেবারে সামঞ্জস্যপূর্ণ নয় (`h2` কুইক-অ্যাক্সেস, কিন্তু লিডারবোর্ড/স্ট্রিক `h3`, মাঝে `h2` নেই)।
18. প্রোমো ব্যানার `banner.png`-এর `alt="Promo Banner"` — ইংরেজি ও অর্থহীন; পুরো ড্যাশবোর্ডে একমাত্র র‍্যাস্টার ফুল-ব্লিড ইমেজ, ল্যান্ডিংয়ের ভেক্টর/কম্পোনেন্ট-ভিত্তিক ভিজ্যুয়াল ভাষার সাথে বেমানান (ডার্ক মোডেও একই সাদা ব্যানার)।

---

## ✅ যা ঠিক আছে
- ব্র্যান্ড কমলা `#ff5200` ও গোল্ড `#ffb92e` দুই পেজেই মূল অ্যাকসেন্ট (মোডাল বাদে)।
- `focus-ring` ইউটিলিটি `.pk-landing` বেসে আছে, তাই দুই পেজেই কাজ করে — ড্যাশবোর্ডে ৭ জায়গায় ব্যবহৃত।
- `noise`, `ring-conic`, `glass` বেস স্তরে থাকায় শেয়ারযোগ্য; ড্যাশবোর্ড হিরো ল্যান্ডিং CTA সেকশনের ডার্ক প্যানেল ভাষা অনুসরণ করে (`noise` + কনিক গ্লো) — এটাই সবচেয়ে ভালো সংযোগ বিন্দু।
- বাংলা সংখ্যা সর্বত্র (`toBn` / `toBengaliNumber`) — যদিও দুটো আলাদা হেল্পার।

---

## প্রস্তাবিত ফিক্স-অর্ডার
1. `ink.DEFAULT` ও `ink.950` একীভূত করে একটাই ওয়ার্ম র‍্যাম্প; `paper` একটাই হেক্স।
2. `.pk-landing .font-display` থেকে `:not(.dash)` তুলে দাও (এক লাইনের বাগফিক্স)।
3. স্ট্রিক মোডালের সব হার্ডকোডেড হেক্স → `brand/gold/ink` টোকেন, এবং লাইট-মোড ভ্যারিয়েন্ট; ক্যালেন্ডার রিয়েল ডেটায়।
4. ড্যাশবোর্ড প্রাইমারি CTA → ল্যান্ডিংয়ের কমলা গ্রেডিয়েন্ট পিল; `lime` শুধু accent।
5. `Navigation.tsx`-এর gray/zinc/orange → paper/ink/brand টোকেন।
6. `EASE` ও `toBn/toBengaliNumber` একটাই শেয়ার্ড মডিউলে।
7. `.dash` স্তরে `text-mist` সহ ডার্ক ওভাররাইড যোগ; মাইক্রো-লেবেলের অপাসিটি ≥70%।
8. রেডিয়াস স্কেল টোকেনাইজ (`rounded-card`, `rounded-panel`) এবং রুট ফন্ট-সাইজ একীকরণের সিদ্ধান্ত।

---

# ✅ কী কী ঠিক করা হলো

## P0
1. **এক ওয়ার্ম ink র‍্যাম্প** (`tailwind.config.js`) — `ink.950` এখন `#161210`, অর্থাৎ `ink.DEFAULT`-এর সমান; পুরো 50→950 স্কেল কুল ধূসর থেকে ওয়ার্ম করা হয়েছে। `text-ink` আর `text-ink-950` একই কালো দেখায়।
2. **এক paper/এক dark-bg** (`index.css`) — `.pk-landing:not(.dash)` এর `#fafaf9/#101018` → `#faf9f6/#161210`; ডার্কে `#0e0b09` → `#161210`, যা `.dash`-এর সমান।
3. **প্রাইমারি CTA একীভূত** — ড্যাশবোর্ডের "মডেল টেস্ট দিন" এখন ল্যান্ডিং হিরোর মতোই `bg-gradient-to-r from-brand-600 to-brand-500` + সাদা টেক্সট + `btn-shine` + `hover:-translate-y-1`। `lime` আবার শুধু accent।
4. **স্ট্রিক মোডাল সম্পূর্ণ পুনর্লিখিত** — `#131F24/#FF9600/#FFB800/#38BDF8` সব বাদ (এখন শূন্য হার্ডকোডেড হেক্স); `bg-paper dark:bg-ink-2` দিয়ে **লাইট ও ডার্ক দুই মোডেই** কাজ করে; `role="dialog"`/`aria-modal` যোগ।
5. **ক্যালেন্ডার রিয়েল ডেটায়** — হার্ডকোডেড "জুন ২০২৬" + ৩০ সেল + `isToday = i === 6` বাদ। এখন `calendarCells` আসল মাসের দৈর্ঘ্য, সঠিক leading blank (সপ্তাহের শুরু), আসল `todayKey`, আর `activityLog` থেকে active দিন হিসাব করে। মাস-নেভিগেশন বাটন দুটো এখন সত্যিই কাজ করে (ভবিষ্যতের মাসে যাওয়া নিষ্ক্রিয়)।
6. **`Navigation.tsx` শেল ওয়ার্ম প্যালেটে** — ৬৪টি `gray-/zinc-/orange-/red-` ক্লাস → `paper/ink/mint/brand/flag` টোকেন। ডার্ক মোডে আর "খাঁটি কালো নেভ + বাদামি কনটেন্ট" নয়।

## P1
7. **`font-display` বাগ ফিক্স** — `.pk-landing:not(.dash) .font-display` → `.pk-landing .font-display`। ড্যাশবোর্ডের বাংলা সংখ্যা আর গ্লিফহীন Bricolage/Inter-এ পড়ে না।
8. **সেরিফ হেডিং বাদ** — ড্যাশবোর্ডের ৩টি `font-bangla` (Noto Serif Bengali) হেডিং → `font-display` (Anek Bangla), ল্যান্ডিংয়ের সাথে মিল। `font-bangla` এখন ০ বার ব্যবহৃত।
9. **এক লোগো লকআপ** — ড্যাশবোর্ডের আলাদা `letterlogo.svg`/`letterlogo-white.svg` জোড়ার বদলে ল্যান্ডিংয়ের শেয়ার্ড `LogoMark` + `পরীক্ষা<span class="text-gradient">ঙ্গন</span>` wordmark।
10. **ওজন স্বাভাবিক** — `font-black` → `font-bold` হেডিংয়ে, ল্যান্ডিংয়ের টাইপ ওজনের সাথে সঙ্গতিপূর্ণ।

## P2
11. **শেয়ার্ড ইউটিলিটি আনস্কোপড** — `text-gradient`, `text-gradient-dark`, `card-ring`, `card-shine`, `btn-shine`, `glow-brand`, `dot-grid` এখন `.pk-landing` স্তরে, তাই ড্যাশবোর্ডেও কাজ করে (আগে `:not(.dash)`-এ আটকে ছিল)।
12. **রেডিয়াস টোকেনাইজড** — নতুন `rounded-card (1.6rem)` / `rounded-panel (2rem)` / `rounded-hero (2.5rem)`; ল্যান্ডিং ও ড্যাশবোর্ড দুই জায়গাতেই অ্যাড-হক মানগুলো এই স্কেলে ম্যাপ করা।
13. **মোশন টোকেন একীভূত** — নতুন `utils/motionTokens.ts` (`EASE`, `DURATION`, `TRANSITION_CLASS`)। ডুপ্লিকেট `EASE` দুটো সরিয়ে দুই পাশেই এখান থেকে import; `toBn` এখন `utils/numberUtils`-এর `toBengaliNumber`-এর re-export (দুটো আলাদা ইমপ্লিমেন্টেশন আর নেই)। হোভার টাইমিং `duration-300` → `duration-500`, লিফট `-translate-y-1.5` — ল্যান্ডিংয়ের সমান।
14. **থিম-টগল দ্বন্দ্ব** — ল্যান্ডিং টগল এখন বেছে নেওয়া থিম OS-এর সাথে মিললে `"system"` লেখে, তাই ইউজারের `system` পছন্দ আর স্থায়ীভাবে মুছে যায় না।

## অ্যাক্সেসিবিলিটি
15. **`text-mist` কনট্রাস্ট** — টোকেন `#6f655c` → `#6a6058` (paper-এ **5.82:1**, আগে 5.4); এবং `.dash`-এর জন্য ডার্ক-মোড ওভাররাইড যোগ (`#b0a498`, ink-এ **7.63:1**) — আগে ড্যাশবোর্ডে ডার্কে কোনো ওভাররাইডই ছিল না (~2.3:1 ফেল)।
16. **হিরোর মাইক্রো-লেবেল** — `text-white/50` → `text-white/75`, আর `text-[9px]` → `text-[10px]/[11px]`।
17. **হেডিং হায়ারার্কি** — লিডারবোর্ডের `h3` → `h2` (টপ-লেভেল সেকশন)।
18. **ব্যানার alt** — `"Promo Banner"` → অর্থবহ বাংলা বিবরণ।

## যাচাই
- `npx tsc --noEmit` — ০ এরর
- `npm run test` — ২৭/২৭ পাস
- `npm run build` — সফল (৩৬১টি স্ট্যাটিক পেজ সহ)
- হার্ডকোডেড হেক্স `#131F24/#FF9600/#FFB800/#38BDF8` — ০টি অবশিষ্ট
- `Navigation.tsx`-এ `gray-/zinc-/orange-/red-` — ০টি অবশিষ্ট

## এখনো বাকি (ইচ্ছাকৃতভাবে)
- **রুট ফন্ট-সাইজ** (ল্যান্ডিং 16px বনাম অ্যাপ 15px/13.5px) ইচ্ছাকৃতভাবে রাখা হয়েছে — অ্যাপ স্ক্রিনগুলো ঘনত্বের জন্য ছোট বেসে ডিজাইন করা, এটি বদলালে প্রতিটি অ্যাপ পেজ রি-টিউন করতে হবে। আলাদা কাজ হিসেবে করা উচিত।
- প্রোমো `banner.png` এখনো র‍্যাস্টার ও ডার্ক মোডে একই ছবি — ডার্ক ভ্যারিয়েন্ট আর্টওয়ার্ক দরকার (ডিজাইন অ্যাসেট)।
